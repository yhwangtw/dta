apiVersion: v1
kind: Service
metadata:
  name: dta-agent-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dta-agent-platform
  ports:
    - name: http
      port: 80
      targetPort: http
